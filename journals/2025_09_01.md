# [[RIA (Recipe Import Assistant)]]
	- It has been a couple days now of trying to come up with a way to store 'CVS' files.
		- The recipe-id needs to be 100% unique for this to work.  Currently it is inly unique to the ingredient list.  If there are duplicate recipes there are also duplicate recipe-id.  
		  logseq.order-list-type:: number
			- This is currently managed in level-7 (export) and I would need to move that back to level-6 (encode).
			  logseq.order-list-type:: number
			- The complication in doing  this is that the modified recipe-id is generated by looking for duplicate filenames. (The recipe-id is used as a filename.)
			  logseq.order-list-type:: number
			- To accomplish generating a unique recipe-id to level-6 is that I will need something to remember all the recipe-id used. (Like a database.)  While this can be done it is more work that I think I want to do.
			  logseq.order-list-type:: number
		- I could create a new program that would use the existing output files and translate them to CVS.
		  logseq.order-list-type:: number
			- While almost all the code to do this is already written, it is part of the core program not a library function.  The current program would have to be disassembled and rebuilt into a new one.  Again a lot of work.
			  logseq.order-list-type:: number
			- Some highbred of the above.
			  logseq.order-list-type:: number
				- Create a level-8 to build the CVS files.  This is kind of kludgy.
				  logseq.order-list-type:: number